# ====================
# Frontend (React + Vite)
# ====================

# Google OAuth 2.0 Client ID (フロントエンド用)
# Google Cloud Console から取得
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# バックエンドAPIのベースURL
# ローカル開発: http://localhost:3001
# 本番環境: https://your-backend-url.vercel.app
VITE_API_BASE_URL=http://localhost:3001

# Google Drive フォルダID（オプション）
# デフォルトで表示するフォルダのID
VITE_GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here
VITE_GOOGLE_DRIVE_FOLDER_ID_2=your_second_folder_id_here

# ====================
# Backend (Express + TypeScript)
# ====================

# Google OAuth 2.0 Client ID (バックエンド用)
# フロントエンドと同じ値
GOOGLE_CLIENT_ID=your_google_client_id_here

# Google OAuth 2.0 Client Secret (バックエンド用)
# ⚠️ このシークレットは公開しないこと！
# Google Cloud Console から取得
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# OAuth 2.0 リダイレクトURI
# ローカル開発: http://localhost:3001/auth/google/callback
# 本番環境: https://your-backend-url.vercel.app/auth/google/callback
# Google Cloud Console の「承認済みのリダイレクトURI」に登録する必要があります
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# フロントエンドのURL（CORS設定用）
# ローカル開発: http://localhost:5173
# 本番環境: https://your-frontend-url.vercel.app
FRONTEND_URL=http://localhost:5173

# サーバーポート
# ローカル開発: 3001
# Vercelでは自動割り当て（この値は無視されます）
SERVER_PORT=3001

# 環境
# development / production
NODE_ENV=development

# セッションシークレット（本番環境で必須）
# ⚠️ 本番環境では必ず安全なランダム文字列を使用してください
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_session_secret_here_at_least_32_characters

# ====================
# 設定方法
# ====================

# 1. このファイルをコピーして .env ファイルを作成:
#    cp .env.example .env

# 2. Google Cloud Console から認証情報を取得:
#    - https://console.cloud.google.com/
#    - OAuth 2.0 クライアントIDを作成
#    - クライアントIDとシークレットをコピー

# 3. .env ファイルの値を実際の値に置き換える

# 4. apps/server/.env も作成:
#    cp .env apps/server/.env

# 5. 本番環境（Vercel）では、Vercelダッシュボードから環境変数を設定:
#    - Project Settings → Environment Variables
#    - 各変数を追加（Production, Preview, Development）

# 詳細は docs/VERCEL_DEPLOYMENT.md を参照してください
